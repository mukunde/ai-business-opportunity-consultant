# Copy to backend/.env and adjust as needed. Never commit the real .env.

ENVIRONMENT=development
DEBUG=true

# Matches the docker-compose Postgres service.
DATABASE_URL=postgresql+psycopg://aiboc:aiboc@localhost:5432/aiboc
